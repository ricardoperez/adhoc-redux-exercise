import { INCREMENT_COUNTER, DECREMENT_COUNTER } from '../actions/counter'

export default function counter(state = 0, action) {
  // TODO implement INCREMENT_COUNTER and DECREMENT_COUNTER
  switch (action.type) {
    default:
      return state
  }
}
